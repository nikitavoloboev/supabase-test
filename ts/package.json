{
  "name": "ts",
  "type": "module",
  "scripts": {
    "s": "bun --watch scripts/run.ts",
    "db:migrate": "drizzle-kit generate && drizzle-kit migrate"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "drizzle-kit": "^0.30.4"
  },
  "peerDependencies": {
    "typescript": "^5.7.3"
  },
  "prettier": {
    "semi": false
  },
  "dependencies": {
    "drizzle-orm": "^0.39.1",
    "postgres": "^3.4.5"
  }
}
